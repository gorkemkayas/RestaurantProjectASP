@page
@model deneme.Pages.LoginModel
@{
    Layout = "_LoginLayout";
    ViewData["Title"] = "Home page";
}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Custom Styling</title>
    <style>
        .custom-input {
            background-color: #2c2c2c;
            color: #ffffff;
        }

            .custom-input::placeholder {
                color: #aaaaaa;
                opacity: 0.8;
            }

            .custom-input:focus {
                background-color: #333333;
                color: #ffffff;
                box-shadow: 0 0 8px rgba(255, 99, 132, 0.5);
                outline: none;
            }
    </style>
</head>

<div class="m-0 p-0 d-flex" style="height:960px;">
    <div class="flex-fill position-relative">
        <div class="position-relative" style="height: 100vh;">
            <video class="video-background" autoplay muted loop>
                <source src="/img/video2.mp4" type="video/mp4">
                Tarayıcınız video etiketini desteklemiyor.
            </video>
        </div>
    </div>

    <div class="col-md-4 d-flex align-items-center justify-content-center" style="background-color:black">
        <div class="form-container p-4 rounded border border-dark p-1" style="background-color:#111111">
            <h3 class="text-center text-white">
                <i class="bi bi-alipay fs-2 me-3 text-danger small"></i>Sign in
                <div class="mt-4 fs-5 text-danger"  asp-validation-summary="ModelOnly"></div>
            </h3>
            <form class="mt-4" method="post">
                <div class="form-group mb-3">
                    <label class="text-white-50 mb-2" asp-for="Email">Email address</label>
                    <input type="email" asp-for="Email" class="custom-input form-control mb-1" placeholder="Enter mail address...">
                    <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>
                </div>
                <div class="form-group mb-3">
                    <label class="text-white-50 mb-2" asp-for="Password">Password</label>
                    <input type="password" asp-for="Password" class="custom-input form-control" placeholder="Enter the password...">
                </div>
                <button type="submit" class="btn btn-outline-danger w-100">Sign in</button>
            </form>
        </div>
    </div>
</div>


